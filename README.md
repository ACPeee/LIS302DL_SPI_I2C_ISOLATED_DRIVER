# LIS302DL_SPI_I2C_ISOLATED_DRIVER
Embedded driver implementation for the LIS302DL accelerometer using STM32F407 Discovery and HAL SPI interface.  The project reads raw acceleration data from the sensor registers and converts it into physical units (g and m/s²).
